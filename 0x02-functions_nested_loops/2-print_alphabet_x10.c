#include "main.h"

/**
 * print_alphabet - prints the lowercase alphabets 10x
 *
 * Return: void
 */
void print_alphabet_x10(void)
{
        char c;
	int i;

	for (i = 0; 1 < 10; 1 ++)
	{
		for (c = 'a'; c <= 'z'; c+=)
		{
                putchar(c);
		}
		putchar('\n');
        }
}
